name: Ascii Workflow

on:
  - push

jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
          - name: checkout Code
            uses: actions/checkout@v2

          -  name: Execute shell script
             run: ./ascii-script.sh